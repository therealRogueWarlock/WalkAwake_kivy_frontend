#:kivy 1.0
<WifiSettings>:
    BoxLayout:
        orientation: 'vertical'

        Label:
            text:'wifi settings'

        Button:
            text: 'back'
            on_press:
                root.manager.transition.direction = 'right'
                root.manager.current = 'settings'